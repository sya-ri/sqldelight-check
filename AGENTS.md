## Rules
- When creating a pull request, create the branch from `release/0.x` and target `release/0.x`.
- Before creating a worktree, check whether an existing worktree is already available. If you are creating a new branch, it is fine to create a worktree for that branch.
- Follow this project's conventions for commits and branches. If those conventions were already researched earlier, reuse that information.
